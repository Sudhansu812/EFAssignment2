@model EFAssignment1.Models.Course

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Course</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CourseTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CourseTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CourseCredits)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CourseCredits)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CourseId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
<h3>Students</h3>
<table class="table">
    <tr>
        <th>Student Name</th>
        <th>Enrollment Date</th>
        <th>Address</th>
    </tr>
    @foreach (var students in Model.Enrollments)
    {
        <tr>
            <td>
                <p>
                    @students.Student.StudentName
                </p>
            </td>
            <td>
                <p>
                    @students.Student.EnrollmentDate
                </p>
            </td>
            <td>
                <p>
                    @students.Student.Address
                </p>
            </td>
        </tr>
    }
</table>
<hr />
<h3>Staff</h3>
<table class="table">
    <tr>
        <th>Staff ID</th>
        <th>Staff Name</th>
    </tr>
    @foreach (var students in Model.Enrollments)
    {
        <tr>
            <td>
                <p>
                    @students.Staff.StaffId
                </p>
            </td>
            <td>
                <p>
                    @students.Staff.StaffName
                </p>
            </td>
        </tr>
    }
</table>
